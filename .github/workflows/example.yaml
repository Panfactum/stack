name: Example
on:
  workflow_dispatch:
defaults:
  run:
    shell: bash

jobs:
  test:
    runs-on: default
    steps:
      - name: "Hello world"
        shell: bash
        run: 'echo "Hello World"'
