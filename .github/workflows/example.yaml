name: Test
on:
  workflow_dispatch:
defaults:
  run:
    shell: bash

jobs:
  test:
    runs-on:
      group: default
    container: node:18
    steps:
      - name: "Hello world"
        shell: bash
        run: 'echo "Hello World"'
