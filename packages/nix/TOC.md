# packages/nix/ Table of Contents

- `localDevShell/` - Nix derivation and scripts that define the local development shell environment. Provides developer tooling, shell hooks, and environment setup.
- `packages/` - Custom Nix package definitions and associated scripts used by the Panfactum framework. Includes build recipes and helper utilities.
