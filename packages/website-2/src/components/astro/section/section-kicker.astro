---
const { class: className } = Astro.props
const onBrand = Astro.props.onBrand ? Astro.props.onBrand : false
---

<h3
  class={`${onBrand ? 'text-brand-secondary' : 'text-primary'} text-sm md:text-md font-semibold ${className || ''}`}
>
  <slot />
</h3>
