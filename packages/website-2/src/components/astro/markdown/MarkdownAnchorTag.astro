---
import { DISCORD_URL, replaceVersionPlaceholders } from "@/lib/constants";

interface Props {
  href: string;
}

const href = replaceVersionPlaceholders(Astro.props.href).replaceAll(
  "__discordServerLink__",
  DISCORD_URL,
);
---

<a {...Astro.props} href={href}><slot /></a>
