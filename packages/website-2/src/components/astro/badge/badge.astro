---
const { onBrand } = Astro.props
---

<div
  class={`${onBrand ? 'bg-brand' : 'bg-primary'} flex items-center gap-2 text-sm lg:text-md h-[28px] border border-[#D0D8DD] rounded-md p-1 h-[32px]`}
>
  <slot />
</div>
