---
interface Props {
  onBrand: boolean;
}

const { onBrand } = Astro.props;
---

<div
  class:list={`${onBrand ? "bg-brand" : "bg-primary"} flex items-center gap-2 text-sm lg:text-md h-[28px] border border-primary rounded-md p-1 h-[32px]`}
>
  <slot />
</div>
