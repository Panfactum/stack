---
const { onBrand } = Astro.props
---

<div
  class={`${onBrand ? 'bg-brand' : 'bg-primary'} flex items-center gap-2 text-sm lg:text-md h-[28px] border border-[#D0D8DD] rounded-md py-1 px-1.5 h-[24px]`}
>
  <slot />
</div>
