---
const { class: className, flex, ...rest } = Astro.props;
---

<section
  class={`container flex flex-col items-center py-7xl max-w-[1200px] px-4 md:px-8 mx-auto ${className || ""}`}
>
  <slot />
</section>
