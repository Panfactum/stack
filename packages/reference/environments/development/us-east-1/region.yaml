# Meta
region: "us-east-1" # The region's name

# AWS
aws_region: "us-east-1" # The AWS region to use for the aws provider
aws_secondary_region: "us-west-2" # The secondary/backup region to use for the aws_secondary provider

# Kubernetes
kube_config_context: "development-seth"
kube_api_server: "https://A22CD77150C602866DD723DDBB7E1BAA.gr7.us-east-1.eks.amazonaws.com"

# Vault
# vault_addr: "https://vault.prod.panfactum.com"

# Extra inputs
extra_inputs:
  enhanced_ha_enabled: false
  vpa_enabled: true
  panfactum_scheduler_enabled: false
  monitoring_enabled: false # ALPHA do not enable
  pull_through_cache_enabled: true

