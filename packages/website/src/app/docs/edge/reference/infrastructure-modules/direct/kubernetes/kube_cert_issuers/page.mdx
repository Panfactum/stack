import ModuleHeader from "../../../ModuleHeader";

{/* lint disable no-duplicate-headings */}

# Kubernetes Certificate Issuers

<ModuleHeader name="kube_cert_issuers" sourceHref="https://github.com/Panfactum/stack/tree/__PANFACTUM_VERSION_EDGE__/packages/infrastructure/kube_cert_issuers" status="stable" type="direct" />

This module provides our standard cluster issuers for [cert-manager](https://cert-manager.io/). It includes:

* Cluster issuer for public domain names

* Cluster issuer for self-signed certs

* Cluster issuer for intermediate CAs

## Providers

The following providers are needed by this module:

* [aws](https://registry.terraform.io/providers/hashicorp/aws/5.39.1/docs) (5.39.1)

* [kubectl](#requirement_kubectl) (2.0.4)

* [kubernetes](https://registry.terraform.io/providers/hashicorp/kubernetes/2.27.0/docs) (2.27.0)

* [vault](https://registry.terraform.io/providers/hashicorp/vault/3.25.0/docs) (3.25.0)

## Required Inputs

The following input variables are required:

### <a name="input_alert_email" /> [alert\_email](#input_alert_email)

Description: An email that will receive certificate alerts.

Type: `string`

### <a name="input_eks_cluster_name" /> [eks\_cluster\_name](#input_eks_cluster_name)

Description: The name of the EKS cluster.

Type: `string`

### <a name="input_vault_internal_url" /> [vault\_internal\_url](#input_vault_internal_url)

Description: The url to the vault instance for internal cert issuance

Type: `string`

## Optional Inputs

The following input variables are optional (have default values):

### <a name="input_aws_iam_ip_allow_list" /> [aws\_iam\_ip\_allow\_list](#input_aws_iam_ip_allow_list)

Description: A list of IPs that can use the service account token to authenticate with AWS API

Type: `list(string)`

Default: `[]`

### <a name="input_namespace" /> [namespace](#input_namespace)

Description: The name of the cert-manager namespace.

Type: `string`

Default: `"cert-manager"`

### <a name="input_route53_zones" /> [route53\_zones](#input_route53_zones)

Description: A mapping of public DNS domains managed by AWS to their configuration; cert-manager uses this to issue public-facing certificates.

Type:

```hcl
map(object({
    record_manager_role_arn = string
    zone_id                 = string
  }))
```

Default: `{}`

### <a name="input_service_account" /> [service\_account](#input_service_account)

Description: The name of the cert-manager service account.

Type: `string`

Default: `"cert-manager"`

## Outputs

The following outputs are exported:

### <a name="output_route53_zones" /> [route53\_zones](#output_route53_zones)

Description: The route53 zone configuration provided as an input

### <a name="output_vault_ca_crt" /> [vault\_ca\_crt](#output_vault_ca_crt)

Description: The public certificate of the root vault certificate authority

## Usage

No notes

{/* lint enable no-duplicate-headings */}
