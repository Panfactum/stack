---
slideLayout: "two-column"
footnotes:

  - "**Interesting project:** [smol machines](https://smolmachines.com/)"

  - "**Interesting project:** [Kubernetes Agent Sandbox](https://agent-sandbox.sigs.k8s.io/docs/)"
---

import { XMark, CheckMark, MinusMark, Checkbox } from "@/pages/presentations/_components/ChecklistIcons";

## Option: Containers / VMs

<div class="columns">
  <div class="column">
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        gap: "16px",
        marginTop: "20px",
      }}
    >
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <XMark /> Allows access to secrets without exposing them
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <MinusMark /> Deny-by-default network access w/ L7 filtering
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <CheckMark /> R/W/X filesystem controls
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <MinusMark /> Supports 100+ concurrent agents
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <XMark /> Shares host utilities
      </div>
    </div>
  </div>
  <div class="column">
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        gap: "16px",
        marginTop: "20px",
      }}
    >
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <CheckMark /> Profiles
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <CheckMark /> Strong enforcement
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <CheckMark /> Cross-platform
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <XMark /> Audit log
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <CheckMark /> FOSS
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <MinusMark /> Easy to use
      </div>
      <div style={{ display: "flex", alignItems: "flex-start" }}>
        <CheckMark /> Easy to share
      </div>
    </div>
  </div>
</div>
