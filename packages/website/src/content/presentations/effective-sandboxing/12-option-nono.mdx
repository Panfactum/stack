---
slideLayout: "two-column"
footnotes:
  - "[nono.sh](https://nono.sh/)"

---

import { CheckMark } from "@/pages/presentations/_components/ChecklistIcons"

## Option: Nono

<div class="columns">
<div class="column">
  <div style={{display: 'flex', flexDirection: 'column', gap: '16px', marginTop: '20px'}}>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Allows access to secrets without exposing them</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Deny-by-default network access w/ L7 filtering</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> R/W/X filesystem controls</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Supports 100+ concurrent agents</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Shares host utilities</div>
  </div>
</div>
<div class="column">
  <div style={{display: 'flex', flexDirection: 'column', gap: '16px', marginTop: '20px'}}>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Profiles</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Strong enforcement</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Cross-platform</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Audit log</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> FOSS</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Easy to use</div>
    <div style={{display: 'flex', alignItems: 'flex-start'}}><CheckMark /> Easy to share</div>
  </div>
</div>
</div>
