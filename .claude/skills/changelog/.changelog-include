# Glob patterns for user-facing paths (one per line).
# Only files matching at least one pattern will appear in changelog diffs.

packages/nix/packages/**
packages/infrastructure/**
packages/cli/**
packages/installer/**
packages/bastion/**
packages/vault/**
flake.nix
flake.lock
